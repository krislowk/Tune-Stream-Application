/*
 * Tune Stream — color palette
 * Purple + teal accent scheme
 */

import 'package:material_ui/material_ui.dart';

// Primary accents — Tune Stream purple
const tunePurple = Color(0xFF9D4EDD);
const tunePurpleLight = Color(0xFFB47AEA);
const tunePurpleDim = Color(0xFF7B2CBF);

// Secondary accent — teal
const tuneTeal = Color(0xFF00D9C0);
const tuneTealLight = Color(0xFF4DE8D4);
const tuneTealDim = Color(0xFF00A896);

// Backgrounds
const tuneBlack = Color(0xFF0A0A0F);
const tuneSurface = Color(0xFF15151C);
const tuneSurfaceAlt = Color(0xFF1E1E2A);
const tuneOutline = Color(0xFF2A2A38);

// Text
const tuneText = Color(0xFFF5F5FA);
const tuneTextMuted = Color(0xFF9A9AA8);

// Accent picker list — kept for Musify's theme switcher
const availableColors = <Color>[
  tunePurple,
  tuneTeal,
  tunePurpleLight,
  tuneTealLight,
  Color(0xFF7B2CBF),
  Color(0xFF00A896),
  Color(0xFF6A4C93),
  Color(0xFF4ECDC4),
  Color(0xFFB47AEA),
  Color(0xFF4DE8D4),
];
